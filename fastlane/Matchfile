git_url(ENV["MATCH_GIT_URL"]) if ENV["MATCH_GIT_URL"]

storage_mode("git")

type("development")

app_identifier(["com.cedricziel.seer"])

git_branch(ENV["MATCH_GIT_BRANCH"]) if ENV["MATCH_GIT_BRANCH"]

# Username/team are read from the Appfile + env vars.
username(ENV["FASTLANE_APPLE_ID"]) if ENV["FASTLANE_APPLE_ID"]

# For more information about the Matchfile, see:
#     https://docs.fastlane.tools/actions/match/
